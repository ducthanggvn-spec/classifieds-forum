const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const cities = await prisma.city.findMany(); console.log('Cities:', cities); const errored = await prisma.post.count({ where: { fbPostId: 'ERROR_API_REJECTED' } }); console.log('Errored posts:', errored); const success = await prisma.post.count({ where: { isPostedToFb: true } }); console.log('Success posts:', success); } main().catch(console.error).finally(() => prisma.$disconnect());
